pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
        stage('build') {
            steps {
                emailext 	subject: "Email subject",
							to: "moises.vidal@celfocus.com",
							body: "Application was built"
            }
        }
    }
}
