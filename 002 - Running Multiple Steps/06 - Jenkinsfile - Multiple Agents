pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
    	stage('Maven') {
		agent {
			docker 'mvn:latest' 
		}
		steps {
			sh 'mvn --version'
		}
	}
	
	stage('NPM') {
		agent {
			docker 'npm:latest' 
		}
		steps {
			sh 'npm --version'
		}
	}
    }
}
