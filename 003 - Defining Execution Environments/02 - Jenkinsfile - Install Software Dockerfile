pipeline {
    agent { 
    	dockerfile {
			dir '02 - Dockerfile - Dockerfile Agent'
		}
	}
    stages {
        stage('build') {
            steps {
            	echo "Don't install curl here!"
                sh "curl www.google.com"
            }
        }
    }
}
